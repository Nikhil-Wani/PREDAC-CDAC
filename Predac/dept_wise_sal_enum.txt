#include<stdio.h>
enum dept{MKT, ADMIN, HR};
struct emp
{
	int id;
	char name[20];
	double sal;
	enum dept d;
};
int main()
{
	int aa=5;
	const int b=aa;

int i,deptno;
double tsal,m,h,a;
	
struct emp e[5];
static int getid=0;
 m=h=a=0.0;
 for(i=0;i<5;i++)
 {  printf("enter name sal and 0 1 2 for dept");
  scanf("%s %lf %d",e[i].name,&tsal,&deptno);
  e[i].sal=tsal;
  switch (deptno)
  {
  case 0:e[i].d=MKT;
	  break;
	  case 1:e[i].d=ADMIN;
	  break;
	  case 2:e[i].d=HR;
	  break;
      default:printf("in valid department");
	  break;
  }
 e[i].id=++getid;
 }


 for(i=0;i<5;i++)
 { 
   printf("\n%d %s %lf %d",e[i].id,e[i].name,e[i].sal,e[i].d);

}
 for(i=0;i<5;i++)
 {
 if(e[i].d==MKT) 
m=m+e[i].sal ;
 else if(e[i].d==HR)

	 h=h+e[i].sal ;
 else if(e[i].d==ADMIN)
	 a=a+e[i].sal ;
 }
 printf("\n%lf %lf %lf",a,h,m);
 getch();
 return 0;
}
