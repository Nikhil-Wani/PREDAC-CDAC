/* Accept number and reverse*/
#include<stdio.h>
int revno(int);
int main()
{ int no,r;
 no=123;
  r=revno(no);
  printf("%d",r);
  getch();
return 0;

}

int revno(int n)
{int rev=0,r;

  while(n!=0)
  { r=n%10;
    rev=rev*10+r;
	n=n/10;
   }
  return rev;

}
